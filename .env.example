# 이 파일을 .env 로 복사한 뒤 값을 채우세요. (.env 는 git에 올라가지 않습니다)
OPENAI_API_KEY=
TAVILY_API_KEY=

# --- 선택 설정 (기본값은 src/patent_rag/config.py 참고) ---
# DATA_DIR=./data
# STORE_DIR=./store
# EMBEDDING_PROVIDER=huggingface
# EMBEDDING_DEVICE=cpu
# CHAT_MODEL=gpt-4-turbo-preview
# SUMMARY_MODEL=gpt-4o-mini
